# Caps the Gradle daemon JVM heap to 512 MB.
# Without this, Gradle has no memory limit and can use 1.5–2 GB during compilation,
# which can crash lower-memory machines.
org.gradle.jvmargs=-Xmx512m -Xms128m
